<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera captures are written to cache/captures and shared with the camera app via
         the app's FileProvider (see AndroidManifest). -->
    <cache-path name="captures" path="captures/" />
</paths>
