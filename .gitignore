# Gradle
.gradle/
build/
*.apk
*.aab

# Kotlin
.kotlin/

# Local configuration (signing keys, api keys, etc.)
local.properties
.buildenv.sh
*.keystore
*.jks

# IntelliJ / Android Studio
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/

# Eclipse
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db

# Nix
result
result-*
.direnv/

# Local tooling / editor state (not part of the project)
.crush/
.crush.db
*.log

# Rust
shared/target/
/target/

# The `generated/` shared type sources ARE committed (regenerable via
# `cargo run --features typegen --bin codegen`); only the built JNI .so
# artifacts are ignored.
generated/jniLibs/

# Gradle wrapper jar is committed so CI can run ./gradlew
